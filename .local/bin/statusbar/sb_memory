#!/bin/sh

div_mebibyte=1048576
mem_raw=$(sysctl -n hw.physmem)
mem_available=$((mem_raw / div_mebibyte))

mem_used=$(top | grep Real | awk '{print $3}' | sed 's:M/.*::g')
printf "[ MEM: %s/%s M ]" $mem_used $mem_available

